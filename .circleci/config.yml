version: 2
jobs:
   build:
     docker:
      - image: nginx:1-perl

     steps:
      - checkout
      - run:
          name: Say hello to the worldinstall dependencies
          command: echo Hello, world!
