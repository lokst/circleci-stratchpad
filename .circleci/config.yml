version: 2
jobs:
   build:
     parallelism: 4
     docker:
       - image: circleci/python:2.7
     steps:
       - checkout
       - run:
          name: Run a subset of tests
          command: find tests -name '*.testfile' | head -n 50 | circleci tests split
       - run:
          name: Say hello to the world
          command: echo "Hello, World!"
