version: 2
jobs:
   build:
     docker:
       - image: circleci/python:2.7
       - image: rabbitmq:3-management
     steps:
       - checkout
       - run: sleep 15
       - run: curl -i -u guest:guest -H "content-type:application/json" -XPUT http://localhost:5672/api/vhosts/premera
       - run: echo "Hello, World!"
