version: 2
jobs:
   build:
     macos:
       xcode: "8.3.3"
     steps:
       - checkout
       - run:
          name: Print the environment
          command: /usr/bin/env
          when: always
       - run:
          name: Say hello to the world
          command: echo "Hello, World!"
