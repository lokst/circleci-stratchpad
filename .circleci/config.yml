version: 2
jobs:
   build:
     docker:
       - image: circleci/python
       - image: circleci/postgres
         name: foobar
         entrypoint: /bin/sh -c "'hostname"
     steps:
       - checkout
       - run:
          name: Say hello to the world
          command: echo "Hello, World!"
       - run:
          name: Print the host name
          command: hostname
